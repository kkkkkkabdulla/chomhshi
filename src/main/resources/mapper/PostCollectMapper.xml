<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.mapper.PostCollectMapper">

    <insert id="insert" parameterType="map">
        INSERT INTO post_collect (user_id, post_id, create_time)
        VALUES (#{userId}, #{postId}, NOW())
    </insert>

    <delete id="delete" parameterType="map">
        DELETE FROM post_collect
        WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <select id="countByPostAndUser" resultType="Integer" parameterType="map">
        SELECT COUNT(*)
        FROM post_collect
        WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <select id="findUserCollects" resultType="Integer" parameterType="Integer">
        SELECT post_id
        FROM post_collect
        WHERE user_id = #{userId}
    </select>

    <select id="findCollectUsers" resultType="Integer" parameterType="Integer">
        SELECT DISTINCT user_id
        FROM post_collect
        WHERE post_id = #{postId}
    </select>

</mapper>