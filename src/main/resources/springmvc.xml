<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
            http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <context:component-scan base-package="com.campus.controller,com.campus.common,com.campus.interceptor"/>

    <context:property-placeholder location="classpath:application.properties"/>

    <mvc:annotation-driven/>

    <bean id="tokenInterceptor" class="com.campus.interceptor.TokenInterceptor"/>
    <bean id="adminTokenInterceptor" class="com.campus.interceptor.AdminTokenInterceptor"/>

    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="maxUploadSize" value="5242880"/>
        <property name="maxInMemorySize" value="40960"/>
    </bean>

    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/api/**"/>
            <mvc:exclude-mapping path="/api/ping"/>
            <mvc:exclude-mapping path="/api/user/wxLogin"/>
            <mvc:exclude-mapping path="/api/admin/**"/>
            <mvc:exclude-mapping path="/api/upload/file/**"/>
            <mvc:exclude-mapping path="/api/post/list"/>
            <mvc:exclude-mapping path="/api/post/detail/**"/>
            <mvc:exclude-mapping path="/api/comment/list/**"/>
            <ref bean="tokenInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/api/admin/**"/>
            <mvc:exclude-mapping path="/api/admin/login"/>
            <ref bean="adminTokenInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <mvc:default-servlet-handler/>

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>
</beans>
